{
  "name": "family-savings-bank",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "emulators": "firebase emulators:start",
    "seed": "npx tsx scripts/seed-emulator.ts",
    "dev:full": "firebase emulators:start &  sleep 3 && npx tsx scripts/seed-emulator.ts && astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "astro": "^7.2.1",
    "firebase": "^11.0.0",
    "fuse.js": "^7.3.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "firebase-admin": "^13.8.0",
    "tailwindcss": "^4.0.0",
    "tsx": "^4.21.0",
    "xlsx": "^0.18.5"
  }
}
